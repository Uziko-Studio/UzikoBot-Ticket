const fs = require("fs");
const Discord = require("discord.js")

module.exports = {

    name: "ticket",
    description: "ticket description",
    permissions: "null",
    dm : false,
    
    async run(bot, msg){

    }
}